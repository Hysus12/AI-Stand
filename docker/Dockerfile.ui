FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md /app/
COPY src /app/src
COPY scripts /app/scripts
RUN pip install --no-cache-dir -e .

EXPOSE 8501
CMD ["streamlit", "run", "src/spbce/ui/app.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
